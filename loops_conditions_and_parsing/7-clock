#!/usr/bin/env bash
# Display clock
hours=0
minutes=1

while [ $hours -lt 13 ]
do
	echo "Hour: $hours"
    while [ $minutes -lt 60 ]
    do
        echo "$minutes"
        minutes=$((minutes+1))
    done
    hours=$((hours+1))
    minutes=1
done
